# Recommended: keep auto and provide a key to use OpenAI at startup.
# For an explicit no-key offline run, set AGENT_MODE=mock and leave the key blank.
# Auto selects once at startup; it is not runtime provider failover.
AGENT_MODE=auto
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
PORT=3000
